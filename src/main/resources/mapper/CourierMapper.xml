<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guet_unknown.bookstoreserver.mvc.mapper.CourierMapper">

    <resultMap type="com.guet_unknown.bookstoreserver.mvc.domain.Courier" id="CourierMap">
        <result property="courierId" column="courier_id" jdbcType="INTEGER"/>
        <result property="orderId" column="order_id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="receiverName" column="receiver_name" jdbcType="VARCHAR"/>
        <result property="receiverStatus" column="receiver_status" jdbcType="VARCHAR"/>
        <result property="receiverTime" column="receiver_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CourierMap">
        select courier_id,
               order_id,
               user_id,
               receiver_name,
               receiver_status,
               receiver_time,
               create_time,
               update_time
        from courier
        where courier_id = #{courierId}
    </select>

    <!--查询所有-->
    <select id="queryAll" resultMap="CourierMap">
        select *
        from courier
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(*)
        from courier
        <where>
            <if test="courierId != null">
                and courier_id = #{courierId}
            </if>
            <if test="orderId != null">
                and order_id = #{orderId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="receiverName != null and receiverName != ''">
                and receiver_name = #{receiverName}
            </if>
            <if test="receiverStatus != null and receiverStatus != ''">
                and receiver_status = #{receiverStatus}
            </if>
            <if test="receiverTime != null">
                and receiver_time = #{receiverTime}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="courierId" useGeneratedKeys="true">
        insert into courier(order_id, user_id, receiver_name, receiver_status, receiver_time, create_time, update_time)
        values (#{orderId}, #{userId}, #{receiverName}, #{receiverStatus}, #{receiverTime}, #{createTime},
                #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="courierId" useGeneratedKeys="true">
        insert into courier(order_id, user_id, receiver_name, receiver_status, receiver_time, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.orderId}, #{entity.userId}, #{entity.receiverName}, #{entity.receiverStatus},
            #{entity.receiverTime}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="courierId" useGeneratedKeys="true">
        insert into courier(order_id, user_id, receiver_name, receiver_status, receiver_time, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.orderId}, #{entity.userId}, #{entity.receiverName}, #{entity.receiverStatus},
            #{entity.receiverTime}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        order_id = values(order_id),
        user_id = values(user_id),
        receiver_name = values(receiver_name),
        receiver_status = values(receiver_status),
        receiver_time = values(receiver_time),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update courier
        <set>
            <if test="orderId != null">
                order_id = #{orderId},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="receiverName != null and receiverName != ''">
                receiver_name = #{receiverName},
            </if>
            <if test="receiverStatus != null and receiverStatus != ''">
                receiver_status = #{receiverStatus},
            </if>
            <if test="receiverTime != null">
                receiver_time = #{receiverTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where courier_id = #{courierId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from courier
        where courier_id = #{courierId}
    </delete>

</mapper>



